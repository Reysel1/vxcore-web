/**
 * X usa la misma imagen que Open Graph. Next no la reutiliza sola: sin este
 * fichero la tarjeta de X se queda sin imagen.
 */
export { default, alt, size, contentType } from "./opengraph-image";
